# AI Video Remix 使用指南

> 这份文档分两部分:**开始之前**是一次性的环境搭建(需要跟着做几步安装/填配置,做完一次就不用再管了);后面**怎么用**开始才是日常操作,那部分是纯聊天,不涉及任何代码。

---

## 一句话说清楚

你给一个视频链接/一段文案,告诉它想要什么风格,它会自己写文案、画分镜、生成视频片段、配好音乐字幕,最后在剪辑软件里拼成一条完整成片——日常使用时你全程只需要在关键节点看两眼、点头确认就行。

---

## 开始之前:搭一次环境(只需要做一次)

第一次用之前,需要先把下面这些东西装好、配好。这一步确实要碰一点技术性的操作(装软件、复制粘贴几条命令、改一个配置文件),但都是照抄照做,不需要看懂原理;做完一次之后,以后每次用都不用再碰这些。

> **注意:`git clone` 只会带来脚本和文档本身,不会带来 `.env` 和 Palmier-Pro 的连接配置。** `.env` 里存的是你自己的账号 key,出于安全原因被排除在仓库之外(不会跟着克隆走);Palmier-Pro 的 MCP 连接存在你自己电脑的客户端设置里(Claude Code 或 Codex,看你用哪个),也不是仓库文件的一部分。也就是说,即使你是照着同事那份仓库原样克隆下来的,下面这几步一样都不能跳——克隆完不代表配置好了。

**你用的是 Claude Code 还是 Codex CLI?** 两边都能用这个 skill,内容完全一样,只是各自去不同的目录找它——用 Claude Code 的同事按 README 把仓库克隆到 `.claude/skills/ai-video-remix/` 就够了;用 **Codex CLI** 的同事,克隆完之后额外执行一条命令建个软链接,让 Codex 也能找到同一份内容(不用也不应该克隆第二份):

```bash
mkdir -p .agents/skills
ln -s ../../.claude/skills/ai-video-remix .agents/skills/ai-video-remix
```

**先装好这些软件**(按顺序装,前一个装完再装下一个,跟用哪个客户端无关):

1. **git**、**Python 3**、**FFmpeg**——用来跑这套流程的基础工具
2. 打开终端,装两个 Python 小插件:`pip install edge-tts`(配音用)和 `pip install Pillow`(封面图用)
3. 如果同事告诉你这次任务要用到"HyperFrames 卡拉OK字幕"效果,额外装 **Node.js**(≥22版本)和 **Chrome**

**再找负责的同事拿两样东西**(这两样是团队共享的,不用自己搭):

1. 一份属于你自己的**网关 key**(用来生成文案、图片的账号凭证)
2. 团队**共享 ComfyUI 集群**的地址(生成视频片段用的算力集群)

**最后把这两样东西填进配置文件**:把 `.env.example` 复制一份改名叫 `.env`,把上一步拿到的 key 和地址填进去对应的位置。

**Palmier-Pro**(最后拼接成片用的剪辑软件)是完全独立的另一套东西,不在这几步里——需要单独找同事要接入方法,而且 **Claude Code 和 Codex 的接入配置是分开的两套**,即使同一个人两边都用,也要各配一次。

具体每一步的命令、`.env` 里每个变量填什么、常见报错怎么办、Codex 兼容的完整说明,都在 [README.md](./README.md) 里——跟着从头做一遍,不确定的地方随时截图问负责环境的同事。

**做完之后怎么确认搭好了**:打开你的客户端(Claude Code 或 Codex),正常聊天没问题、也认识 "palmier-pro" 这个词,就说明环境好了,可以放心往下看,开始日常用了。之后如果它回复说找不到某个 key、连不上某个地址,回头检查一下 `.env` 里是不是漏填了什么,或者问问同事这个值应不应该变过。

---

## 怎么开口

直接用大白话说清楚三件事:**原素材是什么、想要什么风格、大概多长**。不需要任何固定格式或专业术语。

> "把这条视频重新做成海绵宝宝科普风格,60秒左右"
>
> "照我们老胡说说那套流程,给这篇文案做一条视频"
>
> "这段文案帮我做成一条竖屏短视频,风格参考我们AI女友那几条"

它会自动识别你说的是"照着一个现成视频改"还是"从一段文字原创写起",不用你自己说清楚是哪一种。

---

## 过程中会发生什么

整条流程比较长(几十分钟到几小时不等,取决于场景数量),但**大部分时间不需要你守着**,只有下面两个节点它会主动停下来找你确认。

| 顺序 | 阶段 | 你会看到什么 | 你需要做什么 |
|---|---|---|---|
| ① | 写文案 | 一份分镜文案,每一段配一句旁白/台词 | **看一遍故事和人名对不对**——AI 转写容易把同音的名字听错(比如把"章鱼哥"听成"张渔哥"),这一步没抓出来,后面全错 |
| ② | 画首帧图 | 每个场景一张参考画面 | **看一眼人物、画风对不对**——这是动画化之前最后能便宜改的机会,这一步过了才会真正开始烧算力生成视频 |
| ③ | 生成视频片段 | (全自动,不用你盯) | 无需操作,想看进度可以随时问它"现在做到哪了" |
| ④ | 剪辑装配 + 配乐字幕 | (全自动,不用你盯) | 无需操作 |
| ⑤ | 导出成片 | 完成后系统会弹通知 | 去 `~/Downloads/` 文件夹找,文件名就是项目名 |

**这两个确认点不是走过场**——第①步文案错了,后面所有环节都会照着错的内容继续做,到最后才发现就是白干;第②步图错了,视频生成这一步是最烧钱最耗时的环节,提前拦住比生成完再返工便宜得多。除此之外的环节完全可以放手让它自己跑完,不用逐步盯着。

---

## 风格菜单(照抄着说就行)

已经跑通过的风格,直接说"照XX那个风格"就能让它自动套用对应的画幅、画风、配乐方式,不用你自己描述细节:

| 风格 | 大概长什么样 | 配乐怎么来 |
|---|---|---|
| **老胡说说 / 掀牌说**(时事财经点评) | 横屏,厚涂油画质感,一个画面表达一个观点 | 没有配乐,纯口播 |
| **AI女团MV** | 横屏为主,偶像写真或3D玩偶质感,画面跟着真实歌曲对口型 | 用你提供的歌曲本身驱动 |
| **噜噜二创**(IP吉祥物) | 横屏,卡通玩具质感 | 复用原片自带的配乐 |
| **AI建筑**(纪录片式改造) | 竖屏,写实纪录片手持感,镜头之间自然衔接不切黑幕 | 从正版免费曲库配一首 |
| **AI漫剧** | 横屏,插画风或写实风剧情向 | 目前没有配乐 |
| **AI女友 / 宠物喜剧二创** | 竖屏,写实人像 / 3D卡通宠物 | 外部曲库配乐 |
| **AI ASMR** | 竖屏,写实手部特写 | 画面音效联合生成 |

想要一个全新的风格也完全可以,直接描述你想要的效果就行,只是可能需要多花一点时间摸索最合适的做法。

---

## 常见问题

**等了很久没反应,是不是卡住了?**
生成视频片段这一步(上表第③步)最耗时,几十个场景可能要几十分钟到几小时,期间没有进度条很正常。如果超过2小时没任何动静,直接问它"现在进度怎么样了",不用自己重启或者干等。

**生成出来的人物/文字/画面不对怎么办?**
如果是第①②步确认阶段发现的,直接告诉它哪里不对、应该是什么样,它会重新生成那一部分,不用推倒重来整条视频。如果是后面阶段才发现(比如视频片段里出现了一些奇怪的乱码文字或者不相关的画面),同样直接指出来,它知道怎么定位是哪个场景的问题。

**同一个角色在不同视频里长得不一样?**
告诉它你发现的不一致,如果这个角色以后还会反复出现在别的视频里,它会建议把这个角色"存档"下来(建一份角色参考库),以后同一个角色会自动保持一致,不用每次都靠人工挑错。

**做出来的效果不满意,能不能换一版?**
可以,直接说"这个场景/这段不满意,换一个风格再试试",不需要整条视频重做。

**最终视频在哪里?**
默认导出到电脑的 `下载(Downloads)` 文件夹,文件名就是项目名字,完成时电脑会弹系统通知提醒你。

---

## 不用你操心的事

- 不用碰 `.env`、API key、脚本文件——这些都是环境搭建的部分,跟日常用没关系。
- 不用懂"分镜""帧率""ComfyUI"这些词具体是什么意思,它会替你处理所有技术细节。
- 不用担心提需求的措辞"不够专业"——按第2节的大白话范例说清楚"素材、风格、时长"三件事就够了。
